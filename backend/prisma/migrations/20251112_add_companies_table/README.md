This migration creates the `companies` table used by the application.

Files:
- migration.sql : SQL statements to create the `companies` table and indexes.

How to apply:
1. Ensure `DATABASE_URL` in `backend/.env` points to your MariaDB instance.
2. From the `backend/` directory run:
   ```bash
   npx prisma migrate deploy
   ```

If you prefer to create the migration via Prisma locally instead, run:
```bash
npx prisma migrate dev --name add_companies_table
```

Note: This migration was generated by an automated assistant. Review `migration.sql` before applying.
